#include <stdio.h>

/**
 * main - Prints single digit numbers of Base 10 starting from 0.
 * Return:0 (Successful)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}

